import { LightningElement } from "lwc";
import { BaseLayout } from "vlocity_ins/baseLayout";
import temp from "./climsCardCanvas1x.html";
export default class climsCardCanvas1x extends BaseLayout(LightningElement) {
  render() {
    return temp;
  }
}